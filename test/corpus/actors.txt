================
Empty actor
================

actor Counter do
end

---

(source_file
  (actor_definition
    (upper_identifier)))

================
Actor with untyped state
================

actor Counter do
  state count: 0
end

---

(source_file
  (actor_definition
    (upper_identifier)
    (state_definition
      (typed_field_list
        (typed_field
          (identifier)
          (integer))))))

================
Actor with multiple untyped state fields
================

actor Account do
  state balance: 0, name: "unknown"
end

---

(source_file
  (actor_definition
    (upper_identifier)
    (state_definition
      (typed_field_list
        (typed_field
          (identifier)
          (integer))
        (typed_field
          (identifier)
          (string))))))

================
Actor with typed state
================

actor Counter do
  state count: Int :: 0
end

---

(source_file
  (actor_definition
    (upper_identifier)
    (state_definition
      (typed_field_list
        (typed_field
          (identifier)
          (type_name
            (upper_identifier))
          (integer))))))

================
Actor with typed list state
================

actor Cart do
  state items: [Item] :: []
end

---

(source_file
  (actor_definition
    (upper_identifier)
    (state_definition
      (typed_field_list
        (typed_field
          (identifier)
          (type_name
            (upper_identifier))
          (list))))))

================
Actor with typed state no default
================

actor A do
  state name: String
end

---

(source_file
  (actor_definition
    (upper_identifier)
    (state_definition
      (typed_field_list
        (typed_field
          (identifier)
          (type_name
            (upper_identifier)))))))

================
Actor with message handler
================

actor Counter do
  on :increment do
    become count: count + 1
    reply :ok
  end
end

---

(source_file
  (actor_definition
    (upper_identifier)
    (message_handler
      (atom)
      (become_statement
        (key_value_list
          (key_value_pair
            (identifier)
            (binary_expression
              (identifier)
              (integer)))))
      (reply_statement
        (atom)))))

================
Handler with parameters
================

actor Cart do
  on :add(item) do
    reply :ok
  end
end

---

(source_file
  (actor_definition
    (upper_identifier)
    (message_handler
      (atom)
      (parameter_list
        (identifier))
      (reply_statement
        (atom)))))

================
Handler with multiple parameters
================

actor Chat do
  on :send(user, text) do
    reply :ok
  end
end

---

(source_file
  (actor_definition
    (upper_identifier)
    (message_handler
      (atom)
      (parameter_list
        (identifier)
        (identifier))
      (reply_statement
        (atom)))))
